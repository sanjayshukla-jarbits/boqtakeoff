<Window x:Class="boqtakeoff.ui.Views.FamilyBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BIM Library Browser" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style x:Key="TreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="False"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <StackPanel>
                <TextBlock Text="BIM Library Browser" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          Foreground="White"/>
                <TextBlock Text="Browse and add Revit families from the cloud library" 
                          FontSize="12" 
                          Foreground="White" 
                          Opacity="0.9"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" 
                        Grid.Column="0"
                        Height="30" 
                        VerticalContentAlignment="Center"
                        Padding="5"
                        FontSize="12"
                        BorderBrush="#BDBDBD"
                        BorderThickness="1">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <Button x:Name="btnSearch" 
                       Grid.Column="1"
                       Content="Search" 
                       Width="80"
                       Height="30"
                       Margin="5,0,0,0"
                       Background="#2196F3"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Click="btnSearch_Click"/>

                <Button x:Name="btnRefresh" 
                       Grid.Column="2"
                       Content="Refresh" 
                       Width="80"
                       Height="30"
                       Margin="5,0,0,0"
                       Background="#4CAF50"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Click="btnRefresh_Click"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="300" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <!-- Folder TreeView -->
            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" 
                              Text="Categories" 
                              FontWeight="Bold" 
                              FontSize="14"
                              Padding="10"
                              Background="#FAFAFA"/>
                    
                    <TreeView x:Name="treeViewFolders"
                             ItemContainerStyle="{StaticResource TreeViewItemStyle}"
                             SelectedItemChanged="treeViewFolders_SelectedItemChanged"
                             BorderThickness="0">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📁 " FontSize="14"/>
                                    <TextBlock Text="{Binding Name}" FontSize="12"/>
                                    <TextBlock Text="{Binding FileCount, StringFormat=' ({0})'}" 
                                              FontSize="11" 
                                              Foreground="Gray" 
                                              Margin="5,0,0,0"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#E0E0E0"/>

            <!-- Family List -->
            <Border Grid.Column="2" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" 
                              Text="Families" 
                              FontWeight="Bold" 
                              FontSize="14"
                              Padding="10"
                              Background="#FAFAFA"/>
                    
                    <TextBlock x:Name="txtLoadingMessage"
                              DockPanel.Dock="Top"
                              Text="Loading families..."
                              Padding="10"
                              Foreground="Gray"
                              FontStyle="Italic"
                              Visibility="Collapsed"/>

                    <ListView x:Name="listViewFamilies"
                             SelectionMode="Single"
                             SelectionChanged="listViewFamilies_SelectionChanged"
                             BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileName}" 
                                                      ToolTip="{Binding FileName}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Size" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding SizeFormatted}" 
                                                      HorizontalAlignment="Right"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Modified" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd}'}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch" Background="#E0E0E0"/>

            <!-- Details Panel -->
            <Border Grid.Column="4" Padding="10">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                            HorizontalScrollBarVisibility="Disabled"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Details" 
                                  FontWeight="Bold" 
                                  FontSize="14"
                                  Margin="0,0,0,10"/>

                        <StackPanel x:Name="panelDetails">
                            <TextBlock Text="No family selected" 
                                      x:Name="txtNoSelection"
                                      Foreground="Gray" 
                                      FontStyle="Italic"/>

                            <StackPanel x:Name="panelFamilyDetails" Visibility="Collapsed">
                                <TextBlock Text="File Name:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBlock x:Name="txtFileName" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <TextBlock Text="Category:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBlock x:Name="txtCategory" Margin="0,0,0,10"/>

                                <TextBlock Text="Path:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBlock x:Name="txtFolderPath" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <TextBlock Text="Size:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBlock x:Name="txtSize" Margin="0,0,0,10"/>

                                <TextBlock Text="Last Modified:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBlock x:Name="txtLastModified" Margin="0,0,0,10"/>

                                <TextBlock Text="Parameters:" 
                                          FontWeight="SemiBold" 
                                          Margin="0,5,0,2" 
                                          x:Name="txtParametersHeader" 
                                          Visibility="Collapsed"/>
                                
                                <!-- Metadata panel -->
                                <StackPanel x:Name="panelMetadata" Margin="0,0,0,10"/>

                                <Button x:Name="btnAddToProject"
                                       Content="Add to Project"
                                       Height="35"
                                       Margin="0,20,0,0"
                                       Background="#4CAF50"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       BorderThickness="0"
                                       Cursor="Hand"
                                       Click="btnAddToProject_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtStatusMessage" 
                          Grid.Column="0"
                          Text="Ready" 
                          VerticalAlignment="Center"
                          FontSize="11"
                          Foreground="Gray"/>

                <Button Grid.Column="1"
                       Content="Close" 
                       Width="80"
                       Height="25"
                       Background="#757575"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Click="btnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>